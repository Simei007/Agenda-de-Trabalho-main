name: agenda_trabalho
description: App de controle de jornada
version: 1.0.1+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.0.9
  image_picker: ^1.0.4
  path_provider: ^2.1.4
  package_info_plus: ^8.0.2
  shared_preferences: ^2.2.3
  excel: ^2.1.0
  share_plus: ^7.2.2
  qr_flutter: ^4.1.0
  url_launcher: ^6.3.0

dependency_overrides:
  win32: 5.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
